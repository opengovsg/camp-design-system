node_modules
build
build-storybook.log
storybook-static
.DS_Store

**/token-gen/tokens-generated

.turbo

# superpowers specs and plans are working documents, not source artifacts —
# keep them local and uncommitted
docs/superpowers/
.context/
